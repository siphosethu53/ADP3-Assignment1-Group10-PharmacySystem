server:
  error:
    include-stacktrace: never

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/PharmacySystem-Group10-db?createDatabaseIfNotExist=true
    username: root
    password: